import { Router } from 'express';

const router = Router();

// Add unit-related routes here, for example:
// router.get('/', (req, res) => res.json({ message: 'List of units' }));

export default router;