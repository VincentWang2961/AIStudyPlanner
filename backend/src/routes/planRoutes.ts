import { Router } from 'express';

const router = Router();

// Add plan-related routes here, for example:
// router.get('/', (req, res) => res.json({ message: 'List of plans' }));

export default router;