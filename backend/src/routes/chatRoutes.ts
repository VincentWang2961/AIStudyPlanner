import { Router } from 'express';

const router = Router();

// Add chat-related routes here, for example:
// router.get('/', (req, res) => res.json({ message: 'List of chats' }));

export default router;